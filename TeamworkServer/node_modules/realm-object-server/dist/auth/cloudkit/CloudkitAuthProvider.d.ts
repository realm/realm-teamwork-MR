import { User } from "../../realms/AdminRealm";
import { AuthProvider } from "../AuthProvider";
export interface CloudkitAuthProviderParams {
    container: string;
    environment: string;
    keyId: string;
    privateKeyPath: string;
}
export declare class CloudkitAuthProvider extends AuthProvider {
    name: string;
    private options;
    private privateKey;
    constructor(params: CloudkitAuthProviderParams);
    start(): Promise<void>;
    authenticateOrCreateUser(body: any): Promise<User>;
    private signedRequest(path, body);
    private absoluteDatabasePath(path);
    private hash(str);
    private sign(str);
}
