import { User } from "../../realms/AdminRealm";
import { AuthProvider } from "../AuthProvider";
export declare class FacebookAuthProvider extends AuthProvider {
    name: string;
    authenticateOrCreateUser(body: any): Promise<User>;
}
