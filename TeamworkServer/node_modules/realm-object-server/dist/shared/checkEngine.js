"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const semver = require("semver");
function checkEngine(packageJson = "../../package.json") {
    const version = require(packageJson).engines.node;
    if (!semver.satisfies(process.version, version)) {
        console.error(`Required node version ${version} not satisfied with current version ${process.version}.`);
        process.exit(1);
    }
}
exports.checkEngine = checkEngine;
//# sourceMappingURL=checkEngine.js.map