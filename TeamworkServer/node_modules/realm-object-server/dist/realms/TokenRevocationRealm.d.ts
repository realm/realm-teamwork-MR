import { RealmDefinition } from "../RealmFactory";
import * as Realm from 'realm';
export declare class TokenRevocation {
    static schema: Realm.ObjectSchema;
    expires?: Date;
    revoked?: Date;
    token: string;
}
export declare const TokenRevocationRealm: RealmDefinition;
