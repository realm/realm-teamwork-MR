/// <reference types="node" />
import * as event from "events";
export declare class ServiceWatch extends event.EventEmitter {
    serviceName: string;
    serviceTags: string[];
    constructor(name: string, tags?: string[]);
}
